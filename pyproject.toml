[project]
name = "agent-orchestration"
version = "0.1.0"
description = "Harness-agnostic agent orchestration policy and adapters"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "PyYAML==6.0.2",
  "jsonschema==4.25.1",
]
